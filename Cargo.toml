[package]
name = "q"
version = "0.1.0"
edition = "2021"
authors = ["luxluth"]
description = "The Q compiler compiler"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = { version = "4.4.6", features = ["derive"] }
